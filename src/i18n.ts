// Re-export the existing configuration for compatibility
export { locales, defaultLocale } from "./i18n/config";

// Re-export the existing request configuration as default
export { default } from "./i18n/request";
