{% if message %}
<div class="bg-green-900/30 border border-green-500/30 rounded-lg p-4 backdrop-blur-sm">
    <div class="flex items-center">
        <i class="fas fa-check-circle text-green-400 mr-3"></i>
        <span class="text-green-200">{{ message }}</span>
    </div>
</div>
{% endif %} 