{% if message %}
<div class="bg-success/20 border border-success rounded-lg p-4 backdrop-blur-sm">
    <div class="flex items-center">
        <i class="fas fa-check-circle text-success mr-3"></i>
        <span class="text-success">{{ message }}</span>
    </div>
</div>
{% endif %} 